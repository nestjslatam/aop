/** Metadata token of the logger aspect. */
export const LOGGER_ASPECT_TOKEN = 'aop:aspect:logger';

/** Metadata token of the retry aspect. */
export const RETRY_ASPECT_TOKEN = 'aop:aspect:retry';

/** Metadata token of the advice aspect. */
export const ADVICE_ASPECT_TOKEN = 'aop:aspect:advice';

/** Metadata key holding the indexes of the parameters flagged as sensitive. */
export const LOG_REFLECTOR_SENSITIVE = 'LOG_REFLECTOR_SENSITIVE';

/** Value written instead of a sensitive value. */
export const SENSITIVE_MASK = '**********';
